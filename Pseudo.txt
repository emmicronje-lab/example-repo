Request the user to input a positive number 
Store input into variable "number"
Request input until user enters zero value into "number"
 Print the max of all the values entered before the zero value
 Store the max value in the variable "Max".


 Request the user to input their name
 Store input into variable called "first_name"
  Print out "first_name" + "Hello, World".


Create the variable "Total"
Create the variable "Sum"
Create the variable "Average"
Request input of the arbitrary from the user
 Store the numbers given by user into variable "numbers"
 Count the numbers within the "numbers" variable together(SUM)
 Print into "SUM" variable
 Count the total number of "numbers" given
 Print in "Total" variable
If the SUM is greater than zero
 Divide the "SUM" by the "Total" to calculate the average
 Print the "Average"
 

 Request a grocery item from user
 Store input into variable "groceries"
 Request second input from user
 Store the item into "groceries"
 Print out "groceries" in alphabetical order


 Request input X and Y
Store X into "X" variable and Y into "Y" variable
count "X" and "Y" variable together(SUM)
  Print the SUM into "S" variable
Calculate the average by using the SUM and devide by 2
  Print the average into "A" variable
Calculate the product by multiplying "X" and "Y"
  Print the product into "P" variable
Print the "S", "A" and "P" variable
