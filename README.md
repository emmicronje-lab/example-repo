# Calculator application

This respository contains code created during my bootcamp.

#Discription
The calculator application allows users to:
-Perform simple calculations by using addition(+), substraction(-), division(/) and multiplication(*)
-Save all calculations and answers to a text file called 'equations.txt'.
-View previously saved calculations stored in 'equations.txt'.
